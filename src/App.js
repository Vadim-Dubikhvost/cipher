import { Cipher } from './components/Cipher';

function App() {
  return (
    <div style={{display: 'flex',
    alignItems: 'center',
    justifyContent: 'center',
    height: '100vh'}}>
      <Cipher/>
    </div>
  );
}

export default App;
